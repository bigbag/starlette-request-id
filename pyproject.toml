[build-system]
requires = ["hatchling>=1.31.0"]
build-backend = "hatchling.build"

[project]
name = "starlette-request-id"
version = "2.0.0"
description = "Request ID context and response propagation middleware for Starlette"
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
authors = [{ name = "Pavel Liashkov", email = "pavel.liashkov@protonmail.com" }]
keywords = ["asgi", "logging", "request-id", "starlette", "tracing"]
classifiers = [
  "Development Status :: 5 - Production/Stable",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Topic :: Internet :: WWW/HTTP",
  "Topic :: Software Development :: Libraries",
]
dependencies = [
  "request-id-helper>=1.0.0,<2.0.0",
  "starlette>1.2.0,<2.0.0",
]

[dependency-groups]
dev = [
  "httpx2>=2.7.0",
  "mypy>=2.3.0",
  "pytest>=9.1.1",
  "pytest-cov>=7.1.0",
  "ruff>=0.15.22",
]

[project.urls]
Changelog = "https://github.com/bigbag/starlette-request-id/blob/main/CHANGELOG.md"
Homepage = "https://github.com/bigbag/starlette-request-id"
Issues = "https://github.com/bigbag/starlette-request-id/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/starlette_request_id"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.11"
strict = true
warn_return_any = true
warn_unused_ignores = true
files = ["src"]

[[tool.mypy.overrides]]
module = ["request_id_helper"]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-ra --strict-config"

[tool.coverage.run]
source = ["starlette_request_id"]
branch = true
